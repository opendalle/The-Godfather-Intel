# Nexus Asia Intel — requirements
# Versions taken directly from original nexus-asia-distress-radar + nexus-prop-intel repos

# HTTP + parsing
requests==2.31.0
beautifulsoup4==4.12.2
lxml==4.9.4
feedparser==6.0.10

# NLP (exact version from prop-intel repo)
spacy==3.7.2
# GitHub Actions installs model via: python -m spacy download en_core_web_sm

# Database
supabase==2.7.0

# Firecrawl anti-bot bypass (from distress radar repo)
firecrawl-py==1.4.0

# Scheduling
apscheduler==3.10.4

# Environment
python-dotenv==1.0.0

# API
fastapi==0.109.0
uvicorn==0.27.0
httpx==0.27.0
